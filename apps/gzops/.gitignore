# Generated client assets (build:client)
public/assets/
public/vendor/
# Content-hashed copies of tracked assets, emitted by scripts/hash-assets.mjs
public/styles.*.css

# Local env
.env

# Drizzle migration journal is committed; snapshots are too — nothing ignored there
